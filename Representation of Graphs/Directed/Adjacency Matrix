#include <bits/stdc++.h>
using namespace std;

int main() {
	//Representation of undirected graph 
	int n,m;
	cin>>n>>m;
	int adj[n+1][n+1];
	for(int i=0;i<m;i++){
	    int u,v;
	    cin>>u>>v;
//In this graph u and v is connected. 
	    adj[u][v]=1;
	}
    return 0;
}
