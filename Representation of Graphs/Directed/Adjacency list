#include <bits/stdc++.h>
using namespace std;

int main() {
	//Representation of directed graph 
	int n,m;
	cin>>n>>m;
	vector<int> adj[n+1];
	for(int i=0;i<m;i++){
	    int u,v;
	    cin>>u>>v;
//The node u and v is connected in a directed way
	    adj[u].push_back(v);
	}
    return 0;
}
